// Fixture eval suite. The collector should find this directory and the CI reference to it.
export const cases = [
  { name: "summary keeps the decision", input: "…", expect: "…" },
  { name: "triage does not escalate spam", input: "…", expect: "…" },
]
